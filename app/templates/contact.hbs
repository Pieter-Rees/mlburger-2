{{hero-image
    image="contact"
    title="Contact"}}

<main class="container grid-lg">
    {{#each model as |contact|}}
        <div class="columns">
            <div class="column col-12">
                {{markdown-to-html contact.content}}

            </div>
        </div>
    {{/each}}
</main>