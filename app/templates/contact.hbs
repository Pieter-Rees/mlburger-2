{{hero-image image="http://via.placeholder.com/1000x300"}}

{{#each model as |contact|}}
    <div class="columns">
        <div class="column col-12">{{contact.title}}</div>
    </div>

    <div class="columns">
        <div class="column col-12">
            {{markdown-to-html contact.content}}

        </div>
    </div>
{{/each}}