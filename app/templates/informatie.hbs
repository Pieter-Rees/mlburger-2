{{#each model as |informatie|}}
    <div class="columns">
        <div class="column col-12">{{informatie.title}}</div>
    </div>

    <div class="columns">
        <div class="column col-12">
            {{markdown-to-html informatie.content1}}

        </div>
    </div>
     <div class="columns">
        <div class="column col-12">
            {{markdown-to-html informatie.content2}}

        </div>
    </div>
{{/each}}